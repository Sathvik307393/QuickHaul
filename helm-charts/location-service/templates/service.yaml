apiVersion: v1
kind: Service
metadata:
  name: {{ include "location-service.fullname" . }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "location-service.name" . }}
